export const environment = {
    production: false,
    mapbox: {
        accessToken: process.env.REACT_APP_MAPBOX_ACCESS_TOKEN, // Your access token goes here
    },
    // We can create other properties for any other tokens we need
  };